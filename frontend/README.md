# ShopperPet Frontend

This is the frontend package for ShopperPet, scaffolded with [Vite](https://vite.dev/).

## Getting Started

```bash
npm install
npm run dev
```

## Project Structure

- `public/` – Static assets served by the Express backend (HTML pages, CSS, images)
- `src/` – Client-side JavaScript files
